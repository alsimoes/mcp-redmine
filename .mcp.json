{
  "mcpServers": {
    "redmine": {
      "command": "python",
      "args": ["-m", "mcp_redmine"],
      "env": {
        "REDMINE_URL": "http://redmine.ubuntuserver.vmw",
        "REDMINE_API_KEY": "${REDMINE_API_KEY}",
        "REDMINE_TIMEOUT": "15"
      }
    }
  }
}